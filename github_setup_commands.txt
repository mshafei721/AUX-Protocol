# Commands to run after creating GitHub repository
# Replace YOUR_USERNAME with your actual GitHub username

# Add remote origin
git remote add origin https://github.com/YOUR_USERNAME/aux-protocol.git

# Push to GitHub
git branch -M main
git push -u origin main

# Verify the push
git remote -v